# Extension Icons

Replace these placeholder references with real PNG icons (16, 48, 128px) before shipping.

For local development, Chrome will gracefully fall back to the default icon when these files are missing.
